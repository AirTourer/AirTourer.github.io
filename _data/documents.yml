# RETIRED — Document metadata is now managed via front matter in _docs/*.md files.
# The _docs/ collection is the single source of truth for the document catalogue.
# PDF files are stored in documents/{slug}/original.pdf and documents/{slug}/clean.pdf.
# This file is kept as a reference only and is no longer used by the site.
